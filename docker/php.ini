; Upload limits. The app itself allows MAX_UPLOAD_MB (10 by default).
upload_max_filesize = 12M
post_max_size = 14M

; Never print PHP warnings into the page (they would break redirects); log them instead.
display_errors = Off
log_errors = On
